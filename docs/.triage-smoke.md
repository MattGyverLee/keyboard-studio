# Triage gate smoke test

Transient file. Opened to verify that the new `km-triage/review` required check
on ruleset 17331134 actually blocks merge until the km-triage App publishes a
`success` check_run on the head SHA.

Safe to delete in a follow-up PR once the gate behavior is confirmed.

Created 2026-06-06 during the bot-check rollout.
